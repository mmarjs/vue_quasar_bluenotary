export function setUsers(state, users) {
  state.users = users;
}

export function setTokenUser(state, user) {
  state.tokenUser = user;
}
export function setCustomers(state, customers) {
  state.customers = customers;
}
